server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Fallback auf index.html für React Router (SPA)
        try_files $uri $uri/ /index.html;
    }
}